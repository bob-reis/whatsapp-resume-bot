# Copie para .env e preencha antes de rodar `npm run dev`
OPENAI_API_KEY=
# IDs dos grupos separados por vírgula (use `0000000000-000000@g.us`); vazio = todos os grupos
WHATSAPP_TARGET_CHAT_IDS=
# Cron (timezone configurado em TIMEZONE) para rodar o resumo diário
SUMMARY_SCHEDULE=0 20 * * *
# Janela do resumo em minutos (1440 = 24h)
SUMMARY_WINDOW_MINUTES=1440
# Pasta usada para o buffer temporário
BUFFER_PATH=tmp
TIMEZONE=America/Sao_Paulo
# Ajuste apenas se quiser outro modelo compatível
OPENAI_MODEL=gpt-4o-mini
